pipeline {
    agent {
        docker { image 'jenkins-docker-image:v1.1' }
    }git 
    stages {
        stage('Test') {
            steps {
                sh 'docker --version'
            }
        }
    }
}
